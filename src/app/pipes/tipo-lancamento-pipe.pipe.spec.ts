import { TipoLancamentoPipePipe } from './tipo-lancamento-pipe.pipe';

describe('TipoLancamentoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoLancamentoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
